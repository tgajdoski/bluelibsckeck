import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bluelibsCap.com',
  appName: 'bluelibsCap',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
